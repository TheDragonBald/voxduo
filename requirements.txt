# VoxDuo — базовые зависимости.
# Версии закреплены точно: сборка .exe и чужие машины должны получать
# ровно то, что проверено. Обновление — отдельным осознанным коммитом.

# --- Распознавание речи ---
faster-whisper==1.2.1
huggingface-hub==1.30.0

# --- Синтез речи (edge-tts онлайн, Piper офлайн) ---
edge-tts==7.2.8
piper-tts==1.8.0

# --- Аудио ---
sounddevice==0.5.6
soundfile==0.14.0
numpy==2.5.3

# --- Интерфейс ---
customtkinter==6.0.0
pillow==12.3.0
pyperclip==1.11.0
