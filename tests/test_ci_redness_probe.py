"""Временный файл: проверяем, что CI умеет краснеть.

Удаляется следующим коммитом. Здесь две намеренные поломки сразу:
неиспользуемый импорт (нарушение F401 для линтера) и падающий тест.

Смысл в том, чтобы увидеть красным ОБА шага. Если бы линтер и тесты стояли
в одном `run:`, падение линтера утонуло бы в успехе следующей команды — ровно
та дыра, ради которой этот PR и написан.
"""

from __future__ import annotations

import os  # noqa: I001 - намеренно неиспользуемый импорт, линтер обязан ругнуться


def test_ci_must_go_red() -> None:
    assert False, "намеренная поломка: CI обязан покраснеть"
